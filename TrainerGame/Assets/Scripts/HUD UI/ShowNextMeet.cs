using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class ShowNextMeet : MonoBehaviour
{
    //TODO: Code so that the HUD UI always shows what Race Meet is next. 

    private TextMeshProUGUI NextMeetText;

    CalendarManager calendarManager;

    // Start is called before the first frame update
    void Start()
    {
        
    }
    
    void FindNextMeet()
    {
        //TODO: Sort through calendarManager array of events to find the next meet in calendar order
    }
}
